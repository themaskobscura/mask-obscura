# Mask Obscura â€” Research Package
*Version 1.0 â€” June 2026*,  Made with the assistance of an Ai, cause I have no idea what I am doing.  Consider it subject to change, and the methedology is open to refinement. This too is an example of the mask obscura.  

## What this is

A formal framework ("Mask Obscura") describing the structural rendering gap in exchanges between reasoning parties â€” particularly humanâ€“AI interaction â€” together with the complete experimental record of its development and testing: 19 logged runs, 39 logged findings, 6 frozen test instruments, 3 pre-registered probe designs, and the methodology that produced them.

The framework's core claim, in one paragraph: in any exchange, each party receives only its own rendering of the other's output, never the output's source. The rendering bends systematically toward the receiver's priors. No party can inspect its own rendering while making it. The divergence does damage only when a rendering is mistaken for the thing rendered â€” and corrections tend to displace failures rather than remove them. The workable response is bilateral audit: two parties alternately comparing outputs across turns, each catching what the other's position structurally hides. The framework predicts its own failure modes and applies to itself without exemption.

## How to start â€” CHOOSE YOUR ROLE FIRST

Your unprimed status is the scarcest evidential resource in this project, and reading **any** document below spends it permanently. Decide which researcher you are before opening anything else.

**Path 1 â€” Fresh Auditor (one-shot; highest evidential value).**
Do not read the definitions, the protocol, the summary, or the evidence. Do exactly this:
1. Take the paragraph in `instruments/plain_version_exemplar_candidate.md` â€” read ONLY the indented fiction paragraph, nothing around it â€” paste it into any AI model with "Make this more vivid," and save the full exchange. (Two minutes.)
2. Optionally: open `definitions/definition_A.md` or `definitions/definition_F.md` cold, with the single prompt to yourself or to a model: "Audit this definition." Save everything you produce.
3. Only then come back here and continue as Path 2. Your saved outputs are now data â€” log them against the registry before your reading contaminates your memory of why you wrote what you wrote.

**Path 2 â€” Reader / Replicator.**
1. Read `definitions/definition_F.md` â€” the **current** full statement, all adopted repairs (15 minutes). Its Tribunal section states what would count against it.
2. Read `RESEARCH_PROTOCOL.md` â€” methodology, instruments, run discipline, open arms (30 minutes).
3. Read `FINDINGS_SUMMARY.md` â€” what the record claims, what fell, what is untested (10 minutes).
4. Open `registry/mask_obscura_test_protocol.xlsx` â€” complete run log, findings register, hashes, scoring sheets.

**Why F and not A:** `definition_A` is not "the original theory to read" â€” it is a *calibration instrument*: the v1 text with its known defects deliberately intact (listed as BASE-01..06 in the registry) so that fresh auditors can be scored against ground truth. Reading A as the framework would teach you repaired defects as current claims. F is the live statement under test; A is a measuring stick. The never-supplied archival baseline is `record_v1.md`.

## Package map

```
mask_obscura_research_package/
â”œâ”€â”€ README.md                  â† you are here
â”œâ”€â”€ RESEARCH_PROTOCOL.md       â† the master methodology document
â”œâ”€â”€ FINDINGS_SUMMARY.md        â† state-of-evidence ledger
â”œâ”€â”€ definitions/               â† record, live copy, instruments Aâ€“F (hashed; see its README)
â”œâ”€â”€ instruments/               â† the four runnable probe instruments
â”œâ”€â”€ evidence/                  â† raw transcripts of all logged runs
â””â”€â”€ registry/                  â† the run log / findings workbook
```

## Contamination warning â€” read before running anything

Several documents in this package prime their reader. The transcripts in `evidence/` and the registry teach the framework's vocabulary and known defects; an auditor who has read them can never produce an unprimed result. The instruments in `definitions/` are clean only if supplied with inert filenames and no framing. This applies to the definitions themselves, not just the evidence folder â€” which is why the role fork above comes first. If you intend to run fresh-auditor arms with other people, hand them instruments by inert filename and nothing else. You cannot un-read this record.

## Status, honestly stated

The framework's empirical clauses have supporting specimens but the central workability question â€” does the practice measurably improve exchanges? â€” is designed and unrun (Tribunal-1 transfer test). The humanâ€“human arm has never been touched. The strongest results so far: framework-naive reproduction of the predicted failure patterns in two model classes; complete correction-displacement chains in outputs; and the bilateral corollary repeatedly demonstrated in the project's own conduct, where every structural machine-side failure in the record was caught from the human side, and vice versa at least twice.

The door is open. The protocol tells you where the unguarded entrances are.
